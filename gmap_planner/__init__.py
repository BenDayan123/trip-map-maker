"""Google Map Planner — turn a travel itinerary into KML layers + Google My Maps."""

# Single source of truth for the app version. Keep in sync with installer.iss
# `AppVersion` and the git tag (`vX.Y.Z`) on each release — the in-app updater
# compares this against the latest GitHub release tag.
__version__ = "1.1.2"

